{
    "INPUT": {
        "info": "The INPUT block takes user input before the flow is executed and passes it to its outgoing neighbors at the start of execution. All chains of execution start with either an INPUT block, or an INPUT-FIXED block.",
        "maps": {
            "none": [
                "single"
            ]
        },
        "parameters": {},
        "hidden": false
    },
    "INPUT-FIXED": {
        "info": "The INPUT-FIXED block takes a fixed input as a parameter when it is created. When execution is started it passes the input to its outgoing neighbors.",
        "maps": {
            "none": [
                "single"
            ]
        },
        "parameters": {
            "INPUT-FIXED-text": {
                "label": "Fixed Text Input",
                "type": "textbox"
            }
        },
        "hidden": false
    },
    "OUTPUT": {
        "info": "The OUTPUT block forwards any text passed to it to its corresponding text box in the Output section of the sidebar.",
        "maps": {
            "none": [
                "none"
            ],
            "single": [
                "none"
            ]
        },
        "parameters": {},
        "hidden": false
    },
    "COPY": {
        "info": "The COPY block creates an array of n copies of its input, where n is a parameter chosen when the block is created. Note that the COPY block outputs an array.",
        "maps": {
            "none": [
                "none"
            ],
            "single": [
                "multi"
            ],
            "multi": [
                "multi"
            ]
        },
        "parameters": {
            "COPY-num-copies": {
                "label": "Number of Copies",
                "type": "num",
                "min": "2",
                "max": "100",
                "default": "10"
            }
        },
        "hidden": false
    },
    "SPLIT": {
        "info": "The SPLIT block splits its input into overlapping chunks, where the chunk size and overlap is set when the block is created, and outputs these chunks as an array.",
        "maps": {
            "none": [
                "none"
            ],
            "single": [
                "multi"
            ]
        },
        "parameters": {
            "SPLIT-chunk-size": {
                "label": "Chunk Size",
                "type": "num",
                "min": "100",
                "max": "10000",
                "default": "500"
            },
            "SPLIT-chunk-overlap": {
                "label": "Chunk Overlap",
                "type": "num",
                "min": "0",
                "max": "100",
                "default": "0"
            }
        },
        "hidden": false
    },
    "COMBINE": {
        "info": "The COMBINE block exclusively takes arrays as input and concatenates them for its output.",
        "maps": {
            "none": [
                "none"
            ],
            "multi": [
                "single"
            ]
        },
        "parameters": {},
        "hidden": false
    },
    "LLM": {
        "info": "The LLM block calls a Large Language Model according to your API settings and its parameters in order to answer a query. The block has a fixed query, set when it is created; however, if the fixed query contains the string '_INPUT_', then that string is substituted for the input to the block. This acts similarly to a one-input SYNTHESIZE block. The model's response to the constructed query is the output.",
        "maps": {
            "none": [
                "none"
            ],
            "single": [
                "single"
            ],
            "multi": [
                "multi"
            ]
        },
        "parameters": {
            "LLM-query": {
                "label": "Query for LLM",
                "type": "textbox"
            }
        },
        "hidden": false
    },
    "SYNTHESIZE": {
        "info": "The SYNTHESIZE block is a special compound block. It takes an output format as a paramter, which is a string. It consists of n+1 sub-blocks, where n is another parameter set when the block is created. The sub-blocks consist of n numbered inputs all pointing to one output; this output will wait for all of its inputs to come in before executing, entering a yellow waiting state when it has received some inputs but not all. Once all inputs are in, the block will output the result of replacing all instances of the string '_INPUTi_' in the output format parameter with the input from the sub-block labeled input i, for each i from 1 to n. As an example, if n=2 and the output format is '_INPUT1_ (hi!) _INPUT2_', and the inputs are 'Hello' and 'World!', the output will be 'Hello (hi!) World!'.",
        "maps": {
            "none": [
                "none"
            ]
        },
        "parameters": {
            "SYNTHESIZE-num-inputs": {
                "label": "Number of Inputs",
                "type": "num",
                "min": "1",
                "max": "10",
                "default": "2"
            },
            "SYNTHESIZE-output-format": {
                "label": "Output Format",
                "type": "textbox"
            }
        },
        "hidden": false
    },
    "SYNTHESIZE-INPUT": {
        "maps": {
            "none": [
                "none"
            ],
            "single": [
                "none"
            ]
        },
        "parameters": {},
        "hidden": true
    },
    "SYNTHESIZE-OUTPUT": {
        "maps": {
            "none": [
                "none"
            ],
            "single": [
                "single"
            ]
        },
        "parameters": {},
        "hidden": true
    }
}